# Function Lab

A small single-page app for exploring functions as mappings from an input set to an output set.

## Run

```bash
npm install
npm run dev
```

The first version focuses on polynomial and geometry-inspired presets, editable input sets, live output values, and a canvas graph.
